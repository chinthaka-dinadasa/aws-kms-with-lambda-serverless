{
  "name": "aws-kms-with-lambda-serverless",
  "author": "Chinthaka Dinadasa",
  "version": "1.0.0",
  "scripts": {
    "prettier": "npx prettier . --check",
    "prettier_fix": "npx prettier . --write",
    "deploy": "npx prettier . --write && sls deploy",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.10.5",
    "esbuild": "^0.24.2",
    "prettier": "^3.4.2",
    "serverless-plugin-typescript": "^2.1.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@aws-sdk/client-kms": "^3.726.1"
  }
}
